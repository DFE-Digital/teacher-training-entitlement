<h2 class="x-govuk-sub-navigation__heading govuk-heading-s">Statement</h2>
<nav class="x-govuk-sub-navigation" aria-labelledby="sub-navigation-heading">
  <ul class="x-govuk-sub-navigation__section">
    <% items.keys.each do |item| %>
      <%= tag.li(class: item_classes(item)) do %>
        <%= item_link(item) %>
      <% end %>
    <% end %>
  </ul>
</nav>
