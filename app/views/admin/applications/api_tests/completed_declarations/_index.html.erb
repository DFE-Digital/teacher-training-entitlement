<div class="d-flex">
  <h1 class="govuk-heading-m" style="margin: 0;">Completed Declarations API Endpoint</h1>
  <div style="margin-left: 20px;">| <%= render GovukComponent::BackLinkComponent.new(text: "Back", href: admin_applications_api_tests_path(@application)) %></div>
</div>


<div class="govuk-body">  
  <p>The Completed Declarations endpoint creates a 'completed' declaration for a started application.</p>
  <p class="govuk-heading-s">List of delivery partners for <%= @application.lead_provider.name %></p>
  <p>The Completed Declarations endpoint requires a delivery partner to be chosen.</p>
  <%= form_for :form, url: admin_applications_api_tests_completed_declarations_path(@application) do |f| %>
    <%= f.govuk_select :delivery_partner_id, options_for_select(@delivery_partners.map{|dp| [ dp.name, dp.id ]}), label: { text: "Delivery partner", size: "s" } %>
    <%= f.govuk_submit("Call Completed Declarations API") %>
  <% end %>
</div>