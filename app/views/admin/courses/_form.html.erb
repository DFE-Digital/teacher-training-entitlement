<%= form_with model: [:admin, course] do |form| %>
  <%= form.govuk_error_summary %>

  <%=
    form.govuk_text_field :name,
                          width: "two-thirds",
                          label: {
                            text: "Course name",
                            size: "m",
                          }
  %>

  <%=
    form.govuk_text_field :identifier,
                          width: "two-thirds",
                          label: {
                            text: "Course identifier",
                            size: "m",
                          }
  %>

  <%=
    form.govuk_text_area :description,
                         rows: 5,
                         label: {
                           text: "Course description",
                           size: "m",
                         }
  %>

  <div class="govuk-button-group">
    <%= form.govuk_submit course.persisted? ? "Save course details" : "Create course" %>
    <%= govuk_link_to "Cancel", course.persisted? ? admin_course_path(course) : admin_courses_path, class: "govuk-link--no-visited-state" %>
  </div>
<% end %>
