<% content_for(:page_title) { @wizard.step_title } %>

<h1 class="govuk-heading-l"><%= @wizard.step_title %></h1>

<%= form_with model: @wizard.current_step, scope: @wizard.current_step_name, url: @wizard.current_step_path do |f| %>
  <%= f.govuk_error_summary %>

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-half">
      <%= f.govuk_text_field :name,
                             label: { text: "Name", size: "s" } %>
    </div>
    <div class="govuk-grid-column-one-half">
      <%= f.govuk_text_field :identifier,
                             label: { text: "Identifier", size: "s" },
                             placeholder: "Optional" %>
    </div>
  </div>

  <%= f.govuk_text_field :short_code,
                         label: { text: "Short code", size: "s" },
                         placeholder: "Optional" %>

  <%= f.govuk_text_area :description,
                        label: { text: "Description", size: "s" },
                        placeholder: "Optional" %>

  <%= f.govuk_submit "Continue" %>
<% end %>
