<%=
govuk_table do |table|
  table.with_head do |head|
    head.with_row do |row|
      row.with_cell(text: "Course")
      row.with_cell(text: "Recruitment target")
      row.with_cell(text: "Teacher funding")
    end
  end

  table.with_body do |body|
    body.with_row do |row|
      row.with_cell(text: course_name)
      row.with_cell(text: contract.recruitment_target)
      row.with_cell(text: number_to_currency(contract.teacher_funding))
    end
  end
end
%>
