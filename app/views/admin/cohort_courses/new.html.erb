<%= govuk_back_link href: admin_cohort_path(@form.cohort) %>

<h1 class="govuk-heading-l">Add course to cohort <%= @cohort.description %></h1>

<%= form_with model: @form, url: admin_cohort_courses_path(@form.cohort) do |f| %>
  <%= f.govuk_error_summary %>
  <%=
  f.govuk_collection_select(
    :course_id,
    @form.course_options,
    :id,
    :name,
    options: { include_blank: true },
    label: { text: "Course" },
  )
  %>

  <%= f.govuk_date_field :training_starts_at, legend: { text: "Training starts at" }, caption: { text: "Regisration start date: #{@cohort.registration_starts_at.to_fs(:govuk_short)}" } %>
  <%= f.govuk_date_field :training_ends_at, legend: { text: "Training ends at" } %>

  <%= render "admin/course_cohort_providers/lead_providers_form",
             f:,
             lead_provider_options: @form.lead_provider_options,
             selection: @form.selected_lead_providers %>

  <%= f.govuk_submit "Add course" %>
<% end %>
